version: '3.7'

services:
  diid:
    build:
      context: .
      dockerfile: Dockerfile
    image: distinctid
    restart: always
    container_name: cuid
    command: daemon
    environment:
      SHARD_ID: $SHARD_ID
      GUNICORN_WORKERS: $GUNICORN_WORKERS
      JSON_RESPONSE: $JSON_RESPONSE
    # volumes:
    #   - ./sf:/srcfiles
    ports:
    - $SUPERVISOR_IP:9001
    - $WEB_IP:8000
